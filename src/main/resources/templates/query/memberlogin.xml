<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omb.user.login.dao.MemberLoginDAO">

	<select id="memberLogin" resultType="member">
		/* 로그인 처리 쿼리 */
		<!-- select u_no ,u_id , u_name, u_email , u_phone, u_nick, u_phone , u_grade, u_created_at, u_updated_at
		from tb_user
		where u_id =#{u_id} and u_pwd =#{u_pwd} -->
		select A.u_no,add_no ,u_id , u_name, u_email , u_phone, u_nick, u_phone  ,zip, address, sub_address, u_grade, u_status ,u_created_at, u_updated_at
		from tb_user A
		inner join tb_user_address_info B
    	on A.u_no = B.u_no
    	where u_id =#{u_id} and u_pwd =#{u_pwd} and u_status='USING'  	
	</select>
</mapper>